
type Query {
  application: Application!,
}
type Mutation {
  setThemeName(themeName: String!): User!
  setThemeMode(themeMode: String!): User!
  toggleThemeMode: User!
}

type Application {
  name: String!
}

scalar Upload

type Theme {
  name: String
  mode: String
}

type User {
  id: String!
  firstname: String!
  lastname: String!
  profilePicture: String!
  email: String!
  activated: Boolean!
  preferedLanguage: String
  themeSettings: Theme!
}
